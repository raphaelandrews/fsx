{
  "name": "@fsx/db",
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    },
    "./*": {
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:seed": "bun run src/seed.ts",
    "db:migrate": "bun run src/migrate.ts",
    "db:migrate:local": "bun run src/migrate.ts --local"
  },
  "dependencies": {
    "@libsql/client": "catalog:",
    "dotenv": "catalog:",
    "drizzle-orm": "^0.45.2",
    "drizzle-zod": "^0.7.1",
    "libsql": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@fsx/config": "workspace:*",
    "drizzle-kit": "^0.31.10",
    "miniflare": "4.20260424.0",
    "postgres": "catalog:",
    "typescript": "catalog:"
  }
}
